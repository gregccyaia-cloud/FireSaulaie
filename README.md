# Étude incendie - version autonome

Exécuter :

```bash
python -m pip install -r requirements.txt
python main.py
```

Le rapport est généré dans `results/reports/`. Toutes les images sont embarquées dans `assets/`.
